# How the page was merged

Published page: `src/content/docs/docs/structured-output.mdx`, merged on
23 September 2026 from four drafts in `drafts/`.

| Draft | What the page kept |
|---|---|
| `claude/` | The arc (plain, schema, ask, use, trap, fix, program, providers); reading the schema from the outside in; "a schema is what the model is allowed to say" as a foreshadow; the provider table and every number computed from the recordings; the hedgehog exercise; "a record with place other is one a person should look at" |
| `claude-code/` | The schema as a form the model fills in, and "a form with no honest box gets filled in anyway"; each provider's wrong place for the owl, inline; quoting Z.AI's recorded reason and DeepSeek's JSON; "you find out from your program, not from a row that looks fine"; the printed lines after the loop |
| `gpt-6-sol-01a0d014/` | "a valid record, not a true one"; fewer tokens was true of these runs, not a promise; `other` is not a location, keep the observer's words in a field; a schema is a choice about what to keep |
| `schema-and-evidence-170428/` | The table isn't useless, only unstable; the field table before the code; `required` versus merely listing a field; "look at the places: where can the owl go?" before the answer; "a schema controls the shape of an answer, not its truth"; the hares as habitat versus named site; parsing is not checking, a declared type is a promise; the validator exercise; the filing date is not the observation date |

Resolved open questions: Z.AI stays, with the languages named (Rust and Go
were checked for the merge: they behave like Python and TypeScript); the deer
recording is an exercise; the hares stay, as one paragraph.
