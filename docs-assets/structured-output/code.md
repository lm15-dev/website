# structured-output: the verified code

Generated by `scripts/docs-assets-code.ts`; do not edit. Each recipe is what
`<DocsExample recipe="…" />` shows, in the language the reader picked. All of
it has been run in six languages (`npm run test:docs-examples`). The model
shown here, `provider:model`, is replaced by the reader's choice on the page.

Recipes: `so-note`, `so-plain`, `so-schema`, `so-ask`, `so-use`, `so-note-barn`, `so-schema-other`, `so-program`.

## `so-note`

### python

```python
note = """Checked the stream camera this morning. Three
badgers came through overnight, one of them limping.
A fox passed later, just before dawn."""
```

### typescript

```ts
const note = `Checked the stream camera this morning. Three
badgers came through overnight, one of them limping.
A fox passed later, just before dawn.`;
```

### rust

```rust
let note = r"Checked the stream camera this morning. Three
badgers came through overnight, one of them limping.
A fox passed later, just before dawn.";
```

### go

```go
note := `Checked the stream camera this morning. Three
badgers came through overnight, one of them limping.
A fox passed later, just before dawn.`
```

### r

```r
note <- "Checked the stream camera this morning. Three
badgers came through overnight, one of them limping.
A fox passed later, just before dawn."
```

### julia

```julia
note = """Checked the stream camera this morning. Three
badgers came through overnight, one of them limping.
A fox passed later, just before dawn."""
```

## `so-plain`

### python

```python
request = Request(
    model="provider:model",
    system=(
        "You are the field assistant for a wildlife research "
        "station. Turn each field note into sighting records."
    ),
    messages=[Message.user(note)],
)
router = LMRouter()
response = router.complete(request)
print(response.text)
```

### typescript

```ts
const request: Request = {
  model: "provider:model",
  system:
      "You are the field assistant for a wildlife research station. " +
      "Turn each field note into sighting records.",
  messages: [Message.user(note)],
};
const router = new LMRouter();
const response = await router.complete(request);
console.log(response.text);
```

### rust

```rust
let request = Request {
    model: "provider:model".into(),
    system: Some(concat!(
        "You are the field assistant for a wildlife research ",
        "station. Turn each field note into sighting records.",
    ).into()),
    messages: vec![Message::user(note)?],
    ..Default::default()
};
let router = LMRouter::new();
let response = router.complete(&request).await?;
println!("{}", response.text().unwrap_or_default());
```

### go

```go
request := &lm15.Request{
    Model:    "provider:model",
    System:   lm15.System(
        "You are the field assistant for a wildlife research " +
        "station. Turn each field note into sighting records.",
    ),
    Messages: []lm15.Message{lm15.UserMessage(note)},
}
router := lm15.NewRouter()
response, err := router.Complete(context.Background(), request)
if err != nil {
    panic(err)
}
fmt.Println(response.TextOr(""))
```

### r

```r
req <- request(
  "provider:model",
  list(message_user(note)),
  system = paste(
      "You are the field assistant for a wildlife research station.",
      "Turn each field note into sighting records."
  )
)
router <- new_router()
response <- complete(router, req)
response_text(response)
```

### julia

```julia
req = Request(
    "provider:model",
    user(note);
    system=
        "You are the field assistant for a wildlife research " *
        "station. Turn each field note into sighting records.",
)
router = LMRouter()
response = complete(router, req)
println(text(response))
```

## `so-schema`

### python

```python
places = ["oak grove", "stream", "meadow"]
sighting_schema = {
    "type": "object",
    "properties": {
        "sightings": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "species": {
                        "type": "string",
                        "description": "Common name, singular.",
                    },
                    "count": {"type": "integer"},
                    "place": {
                        "type": "string",
                        "enum": places,
                    },
                },
                "required": ["species", "count", "place"],
                "additionalProperties": False,
            },
        },
    },
    "required": ["sightings"],
    "additionalProperties": False,
}
```

### typescript

```ts
const places = ["oak grove", "stream", "meadow"];
const sightingSchema = {
  type: "object",
  properties: {
    sightings: {
      type: "array",
      items: {
        type: "object",
        properties: {
          species: {
            type: "string",
            description: "Common name, singular.",
          },
          count: { type: "integer" },
          place: {
            type: "string",
            enum: places,
          },
        },
        required: ["species", "count", "place"],
        additionalProperties: false,
      },
    },
  },
  required: ["sightings"],
  additionalProperties: false,
};
```

### rust

```rust
let places = ["oak grove", "stream", "meadow"];
let sighting_schema = serde_json::json!({
    "type": "object",
    "properties": {
        "sightings": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "species": {
                        "type": "string",
                        "description": "Common name, singular."
                    },
                    "count": { "type": "integer" },
                    "place": {
                        "type": "string",
                        "enum": places
                    }
                },
                "required": ["species", "count", "place"],
                "additionalProperties": false
            }
        }
    },
    "required": ["sightings"],
    "additionalProperties": false
});
```

### go

```go
places := []any{"oak grove", "stream", "meadow"}
// Keys keep their order: the model fills them in that order.
sightingSchema := lm15.JSONObject{
    lm15.KV("type", "object"),
    lm15.KV("properties", lm15.JSONObject{
        lm15.KV("sightings", lm15.JSONObject{
            lm15.KV("type", "array"),
            lm15.KV("items", lm15.JSONObject{
                lm15.KV("type", "object"),
                lm15.KV("properties", lm15.JSONObject{
                    lm15.KV("species", lm15.JSONObject{
                        lm15.KV("type", "string"),
                        lm15.KV("description",
                            "Common name, singular."),
                    }),
                    lm15.KV("count", lm15.JSONObject{
                        lm15.KV("type", "integer"),
                    }),
                    lm15.KV("place", lm15.JSONObject{
                        lm15.KV("type", "string"),
                        lm15.KV("enum", places),
                    }),
                }),
                lm15.KV("required", []any{
                    "species", "count", "place",
                }),
                lm15.KV("additionalProperties", false),
            }),
        }),
    }),
    lm15.KV("required", []any{"sightings"}),
    lm15.KV("additionalProperties", false),
}
```

### r

```r
places <- json_array("oak grove", "stream", "meadow")
sighting_schema <- json_object(
  type = "object",
  properties = json_object(
    sightings = json_object(
      type = "array",
      items = json_object(
        type = "object",
        properties = json_object(
          species = json_object(
            type = "string",
            description = "Common name, singular."
          ),
          count = json_object(type = "integer"),
          place = json_object(
            type = "string",
            enum = places
          )
        ),
        required = json_array("species", "count", "place"),
        additionalProperties = FALSE
      )
    )
  ),
  required = json_array("sightings"),
  additionalProperties = FALSE
)
```

### julia

```julia
places = ["oak grove", "stream", "meadow"]
sighting_schema = Dict(
    "type" => "object",
    "properties" => Dict(
        "sightings" => Dict(
            "type" => "array",
            "items" => Dict(
                "type" => "object",
                "properties" => Dict(
                    "species" => Dict(
                        "type" => "string",
                        "description" => "Common name, singular.",
                    ),
                    "count" => Dict("type" => "integer"),
                    "place" => Dict(
                        "type" => "string",
                        "enum" => places,
                    ),
                ),
                "required" => ["species", "count", "place"],
                "additionalProperties" => false,
            ),
        ),
    ),
    "required" => ["sightings"],
    "additionalProperties" => false,
)
```

## `so-ask`

### python

```python
request = Request(
    model="provider:model",
    system=(
        "You are the field assistant for a wildlife research "
        "station. Turn each field note into sighting records."
    ),
    messages=[Message.user(note)],
    config=Config(response_format={
        "type": "json_schema",
        "name": "sightings",
        "schema": sighting_schema,
        "strict": True,
    }),
)
router = LMRouter()
response = router.complete(request)
print(response.data)
```

### typescript

```ts
const request: Request = {
  model: "provider:model",
  system:
      "You are the field assistant for a wildlife research station. " +
      "Turn each field note into sighting records.",
  messages: [Message.user(note)],
  config: {
    responseFormat: {
      type: "json_schema",
      name: "sightings",
      schema: sightingSchema,
      strict: true,
    },
  },
};
const router = new LMRouter();
const response = await router.complete(request);
console.log(response.data);
```

### rust

```rust
let format = serde_json::json!({
    "type": "json_schema",
    "name": "sightings",
    "schema": sighting_schema,
    "strict": true
});
let request = Request {
    model: "provider:model".into(),
    system: Some(concat!(
        "You are the field assistant for a wildlife research ",
        "station. Turn each field note into sighting records.",
    ).into()),
    messages: vec![Message::user(note)?],
    config: Config {
        response_format: format.as_object().cloned(),
        ..Default::default()
    },
    ..Default::default()
};
let router = LMRouter::new();
let response = router.complete(&request).await?;
let data = response.data().unwrap_or_default();
println!("{data}");
```

### go

```go
request := &lm15.Request{
    Model:    "provider:model",
    System:   lm15.System(
        "You are the field assistant for a wildlife research " +
        "station. Turn each field note into sighting records.",
    ),
    Messages: []lm15.Message{lm15.UserMessage(note)},
    Config: lm15.Config{
        ResponseFormat: lm15.JSONObject{
            lm15.KV("type", "json_schema"),
            lm15.KV("name", "sightings"),
            lm15.KV("schema", sightingSchema),
            lm15.KV("strict", true),
        },
    },
}
router := lm15.NewRouter()
response, err := router.Complete(context.Background(), request)
if err != nil {
    panic(err)
}
fmt.Println(response.TextOr(""))
```

### r

```r
req <- request(
  "provider:model",
  list(message_user(note)),
  system = paste(
      "You are the field assistant for a wildlife research station.",
      "Turn each field note into sighting records."
  ),
  config = config(response_format = json_object(
    type = "json_schema",
    name = "sightings",
    schema = sighting_schema,
    strict = TRUE
  ))
)
router <- new_router()
response <- complete(router, req)
record <- parse_json(response)
str(record)
```

### julia

```julia
req = Request(
    "provider:model",
    user(note);
    system=
        "You are the field assistant for a wildlife research " *
        "station. Turn each field note into sighting records.",
    config=Config(response_format=Dict(
        "type" => "json_schema",
        "name" => "sightings",
        "schema" => sighting_schema,
        "strict" => true,
    )),
)
router = LMRouter()
response = complete(router, req)
record = parse_json(response)
println(record)
```

## `so-use`

### python

```python
for s in response.data["sightings"]:
    print(s["count"], s["species"], "at", s["place"])
```

### typescript

```ts
type Sighting = { species: string; count: number; place: string };
const data = response.data as { sightings: Sighting[] };
for (const s of data.sightings) {
  console.log(s.count, s.species, "at", s.place);
}
```

### rust

```rust
for s in data["sightings"].as_array().into_iter().flatten() {
    let species = s["species"].as_str().unwrap_or_default();
    let place = s["place"].as_str().unwrap_or_default();
    println!("{} {species} at {place}", s["count"]);
}
```

### go

```go
var record struct {
    Sightings []struct {
        Species string `json:"species"`
        Count   int    `json:"count"`
        Place   string `json:"place"`
    } `json:"sightings"`
}
if err := response.ParseJSON(&record); err != nil {
    panic(err)
}
for _, s := range record.Sightings {
    fmt.Println(s.Count, s.Species, "at", s.Place)
}
```

### r

```r
for (s in record$sightings) {
  cat(s$count, s$species, "at", s$place, "\n")
}
```

### julia

```julia
for s in record["sightings"]
    println(s["count"], " ", s["species"], " at ", s["place"])
end
```

## `so-note-barn`

### python

```python
note = """Around midnight an owl was calling from the old
barn roof, probably a tawny. Two hares in the barn
field at first light."""
```

### typescript

```ts
const note = `Around midnight an owl was calling from the old
barn roof, probably a tawny. Two hares in the barn
field at first light.`;
```

### rust

```rust
let note = r"Around midnight an owl was calling from the old
barn roof, probably a tawny. Two hares in the barn
field at first light.";
```

### go

```go
note := `Around midnight an owl was calling from the old
barn roof, probably a tawny. Two hares in the barn
field at first light.`
```

### r

```r
note <- "Around midnight an owl was calling from the old
barn roof, probably a tawny. Two hares in the barn
field at first light."
```

### julia

```julia
note = """Around midnight an owl was calling from the old
barn roof, probably a tawny. Two hares in the barn
field at first light."""
```

## `so-schema-other`

### python

```python
places = ["oak grove", "stream", "meadow", "other"]
sighting_schema = {
    "type": "object",
    "properties": {
        "sightings": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "species": {
                        "type": "string",
                        "description": "Common name, singular.",
                    },
                    "count": {"type": "integer"},
                    "place": {
                        "type": "string",
                        "enum": places,
                    },
                },
                "required": ["species", "count", "place"],
                "additionalProperties": False,
            },
        },
    },
    "required": ["sightings"],
    "additionalProperties": False,
}
```

### typescript

```ts
const places = ["oak grove", "stream", "meadow", "other"];
const sightingSchema = {
  type: "object",
  properties: {
    sightings: {
      type: "array",
      items: {
        type: "object",
        properties: {
          species: {
            type: "string",
            description: "Common name, singular.",
          },
          count: { type: "integer" },
          place: {
            type: "string",
            enum: places,
          },
        },
        required: ["species", "count", "place"],
        additionalProperties: false,
      },
    },
  },
  required: ["sightings"],
  additionalProperties: false,
};
```

### rust

```rust
let places = ["oak grove", "stream", "meadow", "other"];
let sighting_schema = serde_json::json!({
    "type": "object",
    "properties": {
        "sightings": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "species": {
                        "type": "string",
                        "description": "Common name, singular."
                    },
                    "count": { "type": "integer" },
                    "place": {
                        "type": "string",
                        "enum": places
                    }
                },
                "required": ["species", "count", "place"],
                "additionalProperties": false
            }
        }
    },
    "required": ["sightings"],
    "additionalProperties": false
});
```

### go

```go
places := []any{"oak grove", "stream", "meadow", "other"}
// Keys keep their order: the model fills them in that order.
sightingSchema := lm15.JSONObject{
    lm15.KV("type", "object"),
    lm15.KV("properties", lm15.JSONObject{
        lm15.KV("sightings", lm15.JSONObject{
            lm15.KV("type", "array"),
            lm15.KV("items", lm15.JSONObject{
                lm15.KV("type", "object"),
                lm15.KV("properties", lm15.JSONObject{
                    lm15.KV("species", lm15.JSONObject{
                        lm15.KV("type", "string"),
                        lm15.KV("description",
                            "Common name, singular."),
                    }),
                    lm15.KV("count", lm15.JSONObject{
                        lm15.KV("type", "integer"),
                    }),
                    lm15.KV("place", lm15.JSONObject{
                        lm15.KV("type", "string"),
                        lm15.KV("enum", places),
                    }),
                }),
                lm15.KV("required", []any{
                    "species", "count", "place",
                }),
                lm15.KV("additionalProperties", false),
            }),
        }),
    }),
    lm15.KV("required", []any{"sightings"}),
    lm15.KV("additionalProperties", false),
}
```

### r

```r
places <- json_array("oak grove", "stream", "meadow", "other")
sighting_schema <- json_object(
  type = "object",
  properties = json_object(
    sightings = json_object(
      type = "array",
      items = json_object(
        type = "object",
        properties = json_object(
          species = json_object(
            type = "string",
            description = "Common name, singular."
          ),
          count = json_object(type = "integer"),
          place = json_object(
            type = "string",
            enum = places
          )
        ),
        required = json_array("species", "count", "place"),
        additionalProperties = FALSE
      )
    )
  ),
  required = json_array("sightings"),
  additionalProperties = FALSE
)
```

### julia

```julia
places = ["oak grove", "stream", "meadow", "other"]
sighting_schema = Dict(
    "type" => "object",
    "properties" => Dict(
        "sightings" => Dict(
            "type" => "array",
            "items" => Dict(
                "type" => "object",
                "properties" => Dict(
                    "species" => Dict(
                        "type" => "string",
                        "description" => "Common name, singular.",
                    ),
                    "count" => Dict("type" => "integer"),
                    "place" => Dict(
                        "type" => "string",
                        "enum" => places,
                    ),
                ),
                "required" => ["species", "count", "place"],
                "additionalProperties" => false,
            ),
        ),
    ),
    "required" => ["sightings"],
    "additionalProperties" => false,
)
```

## `so-program`

### python

```python
from lm15 import Config, LMRouter, Message, Request

places = ["oak grove", "stream", "meadow", "other"]
sighting_schema = {
    "type": "object",
    "properties": {
        "sightings": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "species": {
                        "type": "string",
                        "description": "Common name, singular.",
                    },
                    "count": {"type": "integer"},
                    "place": {
                        "type": "string",
                        "enum": places,
                    },
                },
                "required": ["species", "count", "place"],
                "additionalProperties": False,
            },
        },
    },
    "required": ["sightings"],
    "additionalProperties": False,
}

note = """Checked the stream camera this morning. Three
badgers came through overnight, one of them limping.
A fox passed later, just before dawn."""

request = Request(
    model="provider:model",
    system=(
        "You are the field assistant for a wildlife research "
        "station. Turn each field note into sighting records."
    ),
    messages=[Message.user(note)],
    config=Config(response_format={
        "type": "json_schema",
        "name": "sightings",
        "schema": sighting_schema,
        "strict": True,
    }),
)
router = LMRouter()
response = router.complete(request)
print(response.data)

for s in response.data["sightings"]:
    print(s["count"], s["species"], "at", s["place"])
```

### typescript

```ts
import { LMRouter, Message, type Request } from "@lm15/lm15";

const places = ["oak grove", "stream", "meadow", "other"];
const sightingSchema = {
  type: "object",
  properties: {
    sightings: {
      type: "array",
      items: {
        type: "object",
        properties: {
          species: {
            type: "string",
            description: "Common name, singular.",
          },
          count: { type: "integer" },
          place: {
            type: "string",
            enum: places,
          },
        },
        required: ["species", "count", "place"],
        additionalProperties: false,
      },
    },
  },
  required: ["sightings"],
  additionalProperties: false,
};

const note = `Checked the stream camera this morning. Three
badgers came through overnight, one of them limping.
A fox passed later, just before dawn.`;

const request: Request = {
  model: "provider:model",
  system:
      "You are the field assistant for a wildlife research station. " +
      "Turn each field note into sighting records.",
  messages: [Message.user(note)],
  config: {
    responseFormat: {
      type: "json_schema",
      name: "sightings",
      schema: sightingSchema,
      strict: true,
    },
  },
};
const router = new LMRouter();
const response = await router.complete(request);
console.log(response.data);

type Sighting = { species: string; count: number; place: string };
const data = response.data as { sightings: Sighting[] };
for (const s of data.sightings) {
  console.log(s.count, s.species, "at", s.place);
}
```

### rust

```rust
use lm15::{Config, LMRouter, Message, Request};

// Dependencies: lm15, tokio (macros, rt-multi-thread), serde_json
#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let places = ["oak grove", "stream", "meadow", "other"];
    let sighting_schema = serde_json::json!({
        "type": "object",
        "properties": {
            "sightings": {
                "type": "array",
                "items": {
                    "type": "object",
                    "properties": {
                        "species": {
                            "type": "string",
                            "description": "Common name, singular."
                        },
                        "count": { "type": "integer" },
                        "place": {
                            "type": "string",
                            "enum": places
                        }
                    },
                    "required": ["species", "count", "place"],
                    "additionalProperties": false
                }
            }
        },
        "required": ["sightings"],
        "additionalProperties": false
    });

    let note = r"Checked the stream camera this morning. Three
badgers came through overnight, one of them limping.
A fox passed later, just before dawn.";

    let format = serde_json::json!({
        "type": "json_schema",
        "name": "sightings",
        "schema": sighting_schema,
        "strict": true
    });
    let request = Request {
        model: "provider:model".into(),
        system: Some(concat!(
            "You are the field assistant for a wildlife research ",
            "station. Turn each field note into sighting records.",
        ).into()),
        messages: vec![Message::user(note)?],
        config: Config {
            response_format: format.as_object().cloned(),
            ..Default::default()
        },
        ..Default::default()
    };
    let router = LMRouter::new();
    let response = router.complete(&request).await?;
    let data = response.data().unwrap_or_default();
    println!("{data}");

    for s in data["sightings"].as_array().into_iter().flatten() {
        let species = s["species"].as_str().unwrap_or_default();
        let place = s["place"].as_str().unwrap_or_default();
        println!("{} {species} at {place}", s["count"]);
    }
    Ok(())
}
```

### go

```go
package main

import (
    "context"
    "fmt"
    lm15 "github.com/lm15-dev/lm15-go"
)

func main() {
    places := []any{"oak grove", "stream", "meadow", "other"}
    // Keys keep their order: the model fills them in that order.
    sightingSchema := lm15.JSONObject{
        lm15.KV("type", "object"),
        lm15.KV("properties", lm15.JSONObject{
            lm15.KV("sightings", lm15.JSONObject{
                lm15.KV("type", "array"),
                lm15.KV("items", lm15.JSONObject{
                    lm15.KV("type", "object"),
                    lm15.KV("properties", lm15.JSONObject{
                        lm15.KV("species", lm15.JSONObject{
                            lm15.KV("type", "string"),
                            lm15.KV("description",
                                "Common name, singular."),
                        }),
                        lm15.KV("count", lm15.JSONObject{
                            lm15.KV("type", "integer"),
                        }),
                        lm15.KV("place", lm15.JSONObject{
                            lm15.KV("type", "string"),
                            lm15.KV("enum", places),
                        }),
                    }),
                    lm15.KV("required", []any{
                        "species", "count", "place",
                    }),
                    lm15.KV("additionalProperties", false),
                }),
            }),
        }),
        lm15.KV("required", []any{"sightings"}),
        lm15.KV("additionalProperties", false),
    }

    note := `Checked the stream camera this morning. Three
badgers came through overnight, one of them limping.
A fox passed later, just before dawn.`

    request := &lm15.Request{
        Model:    "provider:model",
        System:   lm15.System(
            "You are the field assistant for a wildlife research " +
            "station. Turn each field note into sighting records.",
        ),
        Messages: []lm15.Message{lm15.UserMessage(note)},
        Config: lm15.Config{
            ResponseFormat: lm15.JSONObject{
                lm15.KV("type", "json_schema"),
                lm15.KV("name", "sightings"),
                lm15.KV("schema", sightingSchema),
                lm15.KV("strict", true),
            },
        },
    }
    router := lm15.NewRouter()
    response, err := router.Complete(context.Background(), request)
    if err != nil {
        panic(err)
    }
    fmt.Println(response.TextOr(""))

    var record struct {
        Sightings []struct {
            Species string `json:"species"`
            Count   int    `json:"count"`
            Place   string `json:"place"`
        } `json:"sightings"`
    }
    if err := response.ParseJSON(&record); err != nil {
        panic(err)
    }
    for _, s := range record.Sightings {
        fmt.Println(s.Count, s.Species, "at", s.Place)
    }
}
```

### r

```r
library(lm15)

places <- json_array("oak grove", "stream", "meadow", "other")
sighting_schema <- json_object(
  type = "object",
  properties = json_object(
    sightings = json_object(
      type = "array",
      items = json_object(
        type = "object",
        properties = json_object(
          species = json_object(
            type = "string",
            description = "Common name, singular."
          ),
          count = json_object(type = "integer"),
          place = json_object(
            type = "string",
            enum = places
          )
        ),
        required = json_array("species", "count", "place"),
        additionalProperties = FALSE
      )
    )
  ),
  required = json_array("sightings"),
  additionalProperties = FALSE
)

note <- "Checked the stream camera this morning. Three
badgers came through overnight, one of them limping.
A fox passed later, just before dawn."

req <- request(
  "provider:model",
  list(message_user(note)),
  system = paste(
      "You are the field assistant for a wildlife research station.",
      "Turn each field note into sighting records."
  ),
  config = config(response_format = json_object(
    type = "json_schema",
    name = "sightings",
    schema = sighting_schema,
    strict = TRUE
  ))
)
router <- new_router()
response <- complete(router, req)
record <- parse_json(response)
str(record)

for (s in record$sightings) {
  cat(s$count, s$species, "at", s$place, "\n")
}
```

### julia

```julia
using LM15

places = ["oak grove", "stream", "meadow", "other"]
sighting_schema = Dict(
    "type" => "object",
    "properties" => Dict(
        "sightings" => Dict(
            "type" => "array",
            "items" => Dict(
                "type" => "object",
                "properties" => Dict(
                    "species" => Dict(
                        "type" => "string",
                        "description" => "Common name, singular.",
                    ),
                    "count" => Dict("type" => "integer"),
                    "place" => Dict(
                        "type" => "string",
                        "enum" => places,
                    ),
                ),
                "required" => ["species", "count", "place"],
                "additionalProperties" => false,
            ),
        ),
    ),
    "required" => ["sightings"],
    "additionalProperties" => false,
)

note = """Checked the stream camera this morning. Three
badgers came through overnight, one of them limping.
A fox passed later, just before dawn."""

req = Request(
    "provider:model",
    user(note);
    system=
        "You are the field assistant for a wildlife research " *
        "station. Turn each field note into sighting records.",
    config=Config(response_format=Dict(
        "type" => "json_schema",
        "name" => "sightings",
        "schema" => sighting_schema,
        "strict" => true,
    )),
)
router = LMRouter()
response = complete(router, req)
record = parse_json(response)
println(record)

for s in record["sightings"]
    println(s["count"], " ", s["species"], " at ", s["place"])
end
```
